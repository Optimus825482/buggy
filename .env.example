# Flask Configuration
FLASK_ENV=development
SECRET_KEY=change-this-to-a-random-secret-key-in-production
DEBUG=False

# Application Settings
APP_NAME=Buggy Call
APP_TIMEZONE=Europe/Istanbul
BASE_URL=http://localhost:5000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=buggycall
DB_USER=erkan
DB_PASSWORD=518518Erkan

# JWT Configuration
JWT_SECRET_KEY=change-this-to-a-different-random-secret-key
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=2592000

# CORS Configuration
CORS_ORIGINS=http://localhost:5000,http://localhost:3000

# Redis Configuration (OPTIONAL - Uygulamayı Redis olmadan çalıştırabilirsiniz)
# Redis yoksa otomatik olarak memory cache kullanılır
# Kurulum için: REDIS_KURULUM.md dosyasına bakın
# REDIS_URL=redis://localhost:6379/0
# RATELIMIT_STORAGE_URL=redis://localhost:6379/1
# SOCKETIO_MESSAGE_QUEUE=redis://localhost:6379/0

# Push Notification Configuration (VAPID)
# Generate keys with: python -c "from pywebpush import webpush; print(webpush.generate_vapid_keys())"
VAPID_PUBLIC_KEY=your-vapid-public-key-here
VAPID_PRIVATE_KEY=your-vapid-private-key-here
VAPID_CLAIMS_EMAIL=mailto:admin@yourdomain.com

# Email Configuration (Optional)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-email-password

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/buggycall.log

# File Upload
MAX_CONTENT_LENGTH=16777216

# Rate Limiting
RATELIMIT_ENABLED=True
RATELIMIT_DEFAULT=100 per hour
